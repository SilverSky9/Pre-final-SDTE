package controllers

import (
	"github.com/gofiber/fiber/v2"
)

func PersonRoute(r fiber.Router) {
	// r.Get("/all", func(c *fiber.Ctx) error {
	// msg, error := services.GetAllPerson()

	// if err != nil {
	// 	return c.Status(500).JSON(err.Error())
	// }
	// return c.Send("go fiber is health")
	// })
}
